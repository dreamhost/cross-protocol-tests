# Config: Keys on Alexandria

# Keys on main user:

s3:
    aws_access_key_id: aws_id
    aws_secret_access_key: aws_secret_key
    host: 
    
swift:
    user: username:key
    key: swift_secret_key
    authurl: 

# Keys on second user:

username: second-username

s3user:
    aws_access_key_id: aws_id
    aws_secret_access_key: aws_secret_key
    host:  

swiftuser:
    user: second-username:key
    key: swift_secret_key
    authurl: 